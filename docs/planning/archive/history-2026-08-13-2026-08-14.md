# 历史轮次完成记录归档（2026-08-13 ~ 2026-08-14）

> 本文件归档 `docs/planning/current-handoff.md` 在 2026-08-13 至 2026-08-14 期间各轮次的**已完成代码/验证记录**。
> 归档后 `current-handoff.md` 只保留截至 2026-08-14 的最新状态与必要交接经验。
> 归档仅做记录留存，不代表这些改动已通过完整编译或 GUI 人工验收；验收状态以 `current-handoff.md` 的"已通过检查 / 尚未执行"为准。

---

## 2026-08-13 本轮架构整理

- 修复 `authenticity/repository.rs` 中配置加载的 `Result` 类型推断、查询映射临时借用，以及 `history/repository.rs` 中事务期间错误借用连接的编译问题。
- 将认证发布状态和 `final_artifacts` 原子绑定抽到 `src-tauri/src/authenticity/publication_repository.rs`；认证配置、记录写入和匹配查询仍在 `repository.rs`。
- 将历史破坏性操作收敛到 `src-tauri/src/history/deletion_repository.rs` 的命名能力面，`history/mod.rs` 不再通配符重导出仓储实现。
- `src-tauri/src/storage.rs` 现在是数据库连接配置、路径、时间、ID 和基础校验的共享入口；Library 删除了重复实现并复用它。
- 新增 `docs/architecture/ai-reading-guide.md`，规定按问题选择上下文入口、先读契约再读实现，并记录跨模块边界。

本轮只做了 `cargo fmt --all -- --check` 和 `git diff --check`。`cargo check --lib` 在当前环境超过两分钟未返回，未继续等待；完整编译、测试和 GUI 人工验收仍由用户执行。

## 2026-08-13 本轮认证优化

（占位标题，无实质内容。）

## 2026-08-14 认证与历史 UI 优化

- 修复认证表单浅色背景/灰色文字对比度；全局输入、选择框、文本域补充稳定的文本色、占位符和禁用态颜色。
- 发布、进入发布、取消发布和发布成功后刷新 Artwork 分支，避免发布标签与实际内容慢一拍。
- 私钥改为密码输入，仅在单次发布请求中使用，发布后清空。
- TrustMark 框选自动启用，清空框选自动关闭；JPEG 质量和大小预估移到输出设置区。
- 发布记录进入只读查看模式并提供退出按钮；识别框选增加"改用整图"撤销入口。
- mindmap 增加紧凑/时间轴模式、横向平铺和叶节点分支标签。

本轮未执行完整编译、GUI 流程和大文件测试，待用户按既定验收顺序执行。

- TrustMark 已切换为 Q / BCH_SUPER，ID 为 40 位；仅对用户框选区域编码，不再叠加全图水印。未框选区域时发布请求会自动关闭 TrustMark。
- 发布页增加强度/JPEG 滑条、质量损失提示、大小预览、模型信息摘要；识别页显示完整 C2PA manifest，导出记录自动搜索并展示作者、路径、大小等字段。
- 页面切换保留历史、发布和识别状态；发布模式可确认取消并删除最终成品、发布记录和仓库副本；历史节点增加已发布标识。
- 已完成 `npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml` 和 `git diff --check`。未执行完整 GUI 流程和大文件验收。
- 历史交错状态已做代码级检查：提交、调度、分支删除、节点删除、精简和检查点均继续通过 `final_artifacts`/发布节点约束；解除发布使用统一运行锁。仍需人工覆盖"发布中取消/删除""多分叉后解除发布再提交""区域 TrustMark 发布后整图与框选解码"这些 GUI/真实模型流程。

## 2026-08-14 发布认证与历史图 UI 打磨交接

本轮代码已完成，未执行 GUI 自动化、截图检查或真实 C2PA/TrustMark 流程。用户已明确允许下一轮进行 UI 测试，但本轮到代码、文档和静态验证为止。

### 已完成代码

- 发布记录查看模式改为与发布编辑页一致的左右分栏：左侧认证图片和只读框选范围，右侧锁定的凭证、发布与 TrustMark 字段，下方显示完整 C2PA 报告。
- 发布与识别图片的文件标题栏改为固定内容行，图片舞台占据可伸缩空间，修复大图把"文件名 / 分辨率 / 更换图片"一栏拉到接近图片高度的问题。
- `ArtworkWorkspace` 统一持有当前分支，历史页与发布页双向同步；初始化跳转分支只在导航变化时应用，后续刷新不会把手动选择压回旧分支。
- 表单控件使用统一的 34px 高度、6px 圆角和主题化边框、背景、禁用态颜色；分支名称、备份间隔、提交备注、认证表单和搜索框已覆盖。
- 历史总览标题行新增 220px 到 420px 的节点最小宽度滑条并持久化；紧凑/时间轴切换移到滑条旁边。时间轴按兄弟节点顺序逐列下沉形成阶梯布局，不再按时间戳插入任意高度空白。
- 分支发布总删除入口移到导出记录后的独立危险区，文案明确其删除范围，并改为应用内二次确认。
- JPG 质量、大小预估和透明背景色移到 TrustMark 上方；TrustMark 区增加"在左侧图片拖动框选"的提示，框选/清空继续自动启用/关闭水印。

### 已完成静态验证

- `npm run build`：通过，TypeScript 与 Vite 生产构建成功。
- `git diff --check`：通过，仅输出 Git 的 LF/CRLF 工作区提示。
- 本轮没有改 Rust、数据库 schema 或 Tauri 命令，因此没有运行重依赖 Rust 编译。

## 2026-08-14 截图问题修复与认证记录持久化

- 框选层改为只覆盖图片实际显示矩形，使用 `ResizeObserver` 在舞台或窗口变化时重算；留黑不再参与归一化，区域标签不可被选中。识别页的新拖动会替换唯一旧框。
- 认证记录升级为 schema v6。新发布在首次导出之外原子保存仓库内 JPG 副本；查看和再次导出优先读取副本。v5 旧记录不自动迁移文件，原路径仍存在时继续兼容。
- 记录查看页新增"再次导出"，C2PA 报告与图片预览各自受布局边界约束；缩放窗口时图片和框选一起更新。
- 发布操作完成后显式通知历史模块重新读取 DTO，发布节点标签和计数不再等待轮询。mindmap 兄弟节点间距收紧，标题和节点宽度/视图控制行改为画布内吸顶。
- 私钥非空检查移到点击发布时，通过全局操作提示显示；删除常驻警告行并允许用户点击发布后获得一致错误反馈。
- 已通过 `npx tsc --noEmit`。Rust 只执行 `cargo fmt --manifest-path src-tauri/Cargo.toml --all` 和格式检查；本轮按用户要求未执行完整前端构建、`cargo check/build/test`、GUI 自动化或人工 UI 测试。

## 2026-08-14 UI 源码审计补充

本轮遵守 `docs/guides/validation.md`，没有启动或驱动桌面窗口、截图或执行 GUI 自动化，也没有再次运行任何编译命令。基于现有 TSX/CSS 结构完成以下可静态确认的修复：

- 历史 mindmap 节点卡片不再随整个子树横向拉伸，固定使用节点宽度滑条的值并在子树中居中。
- 修正时间轴模式中下沉节点的竖线终点；连接线现在延伸到节点卡片顶部，不在下沉留白开始处提前结束。
- 历史页窄布局下标题/分支选择、提交区和精简工具栏分行排列，长 Artwork 标题不会挤压右侧控件。
- 发布记录查看页允许输出路径、Manifest 标签和 SHA-256 完整换行；长标题与图片标题不会覆盖退出或更换按钮。
- 发布危险区在单列布局下纵向排列，避免说明文字与删除按钮争抢宽度。
- 新增浅色/深色主题化警告色，统一认证警告和历史未保存状态的对比度；密码输入取消额外字距。

已执行 `git diff --check`，退出码为 0，仅有既存 LF/CRLF 工作区提示。由于项目验证策略禁止代理 GUI 自动化，本节修改仍需用户在 `current-handoff.md` 的人工验收顺序与 GUI 测试清单中确认；不得把本节记为 GUI 已通过。
