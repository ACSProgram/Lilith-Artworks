# 当前任务交接

更新时间：2026-08-15

## 当前阶段：候选前加固完成，等待 Windows 人工验收与 CI

本轮把外部代码审查报告与仓库现状重新对齐。报告中的 LICENSE、公开候选文档和 Windows CI 缺口已经在此前完成：源码现为 GPL-3.0-only，CI 会执行前端生产构建与测试、Rust 格式检查和完整库测试。模型许可也已独立记录；尚未使用 Git LFS、缺少 Windows 代码签名证书仍是分发层待办。

本轮代码已完成，等待人工界面与真实文件流程验收：

- 自动备份失败不再推进 `last_check_ms`，调度器在 60 秒后重试；系统时间读取失败也进入退避，不再静默使用时间戳 `0`。
- 主动提交的相同内容结果改为成功检查提示，并说明内容可能已由刚完成的自动备份记录，不再显示为全局错误。
- 修复审查报告 P0：永久删除已发布 Artwork 时先删除最终成品元数据及其认证记录，再删除历史节点，避免 `ON DELETE RESTRICT` 导致事务失败。
- 发布页新增全分辨率最终 JPG 质量预览，复用正式发布的背景合成、TrustMark 和 JPEG 编码；支持适应窗口、1:1、25% 到 400% 缩放、返回调参和直接签名发布。
- “取消发布并删除本地数据”从页面底部危险区移入页头更多菜单，仍保留明确的危险文案和二次确认。
- 质量预览生成前会检查标题、创作者、证书链和 PEM 私钥；放大后普通滚轮用于纵向浏览，`Ctrl/Command + 滚轮` 缩放，右下角导航小图支持点击和拖动定位。
- P1 路径边界已收紧：仓库成品预览和大小估算只由后端按分支 ID 解析，外部图片预览/识别拒绝仓库路径，发布与再次导出拒绝仓库受控目录；最终成品、证书、外部图片和输出目标还必须存在于 dialog 插件的临时 filesystem scope。应用重启后需重新选择证书，filesystem 插件未向前端授予文件读写命令权限。
- P1 静默降级已清理：历史精简、子树/分支删除的行读取错误向上传播，损坏的认证区域 JSON 不再回退为空数组；`unmark_checkpoint` 的备选查询已确认使用显式错误传播。
- 前端新增 Vitest/jsdom 测试底座，覆盖 Library 树、History 模型、共享格式、预览参数失效、导航几何和 controller latest-request-wins；Windows CI 现同时运行 `npm test`。

## 审查报告处置

- P0-1 已在本轮修复并覆盖定向测试。
- M-2/M-3 路径边界与 M-4 静默吞错进入 P1；M-1 检查点派生规则一致性进入 P2，不与本轮发布主流程混改。
- M-5 全库搜索和 L-2 窗口越界进入 P2 的性能/可用性阶段。
- L-1 路径前缀误判属于“多保留、不误删”的安全方向，L-3 已有待清理队列兜底；两项先记录监测，不高于前端测试、路径边界和错误传播。
- LICENSE、发行文档和 Windows CI 已完成，不再列为缺口；模型 Git LFS 与代码签名保留在 P3。

## 分阶段优化方案

### P0：正确性与发布主流程（本轮，待人工验收）

1. 验收自动备份失败重试、主动提交成功反馈和已发布 Artwork 永久删除。
2. 验收发布质量预览在透明 PNG、不同 JPEG 质量、TrustMark 开关/强度/区域下与实际输出视觉一致。
3. 验收更多菜单和删除确认流程，确认首次导出 JPG 始终保留。

完成标准：下方人工清单全部通过；若发现真实 C2PA/TrustMark 输出与预览像素不一致，继续停留在 P0，不进入公开候选发布。

### P1：公开候选加固（实现完成，等待 CI）

1. 收紧认证路径边界：发布输出拒绝仓库受控目录；梳理任意路径预览/再次导出命令的来源授权，覆盖审查报告 M-2/M-3。
2. 消除剩余静默降级：`delete_subtree` 查询、`unmark_checkpoint` 备选查询和其他 `unwrap_or_default` 统一传播上下文错误，覆盖 M-4。
3. 建立前端测试底座，优先覆盖 `tree.ts`、`historyModel.ts`、共享格式函数、controller 的 latest-request-wins 和发布预览参数失效逻辑。
4. 明确 CI 与本地验证分工：CI 继续运行完整 `cargo test --lib`；本地代理仍按 `validation.md` 只运行定向轻量测试。

完成标准：路径与错误传播测试通过，关键前端纯逻辑和竞态有自动测试，Windows CI 全绿。

本地代码与定向测试已经满足前三项；Windows CI 只能在用户提交并推送后确认，因此当前仍保留最后一道自动门槛。

### P2：可用性与规模基线

1. 为 dialog、tree、历史图和菜单补齐键盘导航、焦点圈、ARIA 关系与读屏文案。
2. 建立大树/大图基线；把 Library 搜索从全表内存过滤下推到 SQLite，并记录支持规模与耗时。
3. 为长任务引入 operation ID/取消协议，避免全局取消误伤相邻任务；补历史临时文件维护命令。
4. 统一检查点存储位与派生规则的命名/测试，避免强制检查点在前端被误解为可取消状态。
5. 加入结构化本地日志和可导出的诊断摘要；窗口恢复增加显示器拓扑越界修正。

完成标准：可访问性人工清单通过，规模基准和支持上限写入文档，长任务能按 operation 精确取消。

### P3：公开分发成熟度

1. 评估 Git LFS 或发布下载方案，避免 ONNX 模型持续扩大普通 Git clone。
2. 获得证书后签名 NSIS 安装包并验证 SmartScreen/升级路径。
3. 根据真实用户需求再安排 macOS/Linux、国际化与匿名诊断，不提前扩大平台范围。

## 自动验证

- `npm run build` 通过，TypeScript 与 Vite 生产构建完成。
- `npm test` 通过：6 个测试文件、14 个用例全部通过。
- `cargo fmt --check --manifest-path src-tauri/Cargo.toml` 通过。
- 定向测试 `permanently_deletes_published_artwork_without_foreign_key_failure` 通过。
- 定向测试 `backup_error_preserves_last_successful_check_for_retry` 通过。
- 定向测试 `rejects_nonexistent_output_below_repository` 通过。
- 定向测试 `subtree_deletion_propagates_branch_row_errors` 通过。
- 定向测试 `invalid_regions_json_is_not_silently_replaced` 通过。
- GUI 自动化按 `docs/guides/validation.md` 禁止；发布预览和菜单需要人工验收。

## 本轮人工回归

1. 把自动备份间隔设为 1 分钟，制造一次可恢复的文件读取失败；错误出现后恢复文件可读性，确认约 60 秒内重试并成功清除错误，而不是等待更长分支间隔。
2. 对未变化文件点击主动提交，确认显示绿色“当前内容已经是最新备份”状态且没有全局错误；修改文件后提交应创建并选中新节点。
3. 先清空 PEM 私钥并点击生成质量预览，确认立即提示且不执行编码；补齐参数后使用透明 PNG 分别生成低/高 JPEG 质量预览。切换适应窗口、1:1、放大和缩小，确认普通滚轮滚动画布、`Ctrl + 滚轮` 缩放，导航小图的视口框可点击和拖动定位；启用 TrustMark 时检查框选区域和自定义/自动 ID，返回调整后应能重新生成。
4. 从预览直接签名发布，使用实际导出 JPG 与预览在 1:1 下比对可见像素质量，并用既有第三方流程回读 C2PA/TrustMark。
5. 确认发布页底部不再显示删除危险区；页头更多菜单能打开删除确认，取消不会改变数据，确认删除后首次导出 JPG 保留。
6. 将一个已有最终成品或认证记录的 Artwork 移入回收站并永久删除，确认不再出现 `FOREIGN KEY constraint failed`，仓库受控目录按清理结果移除。
7. 发布和再次导出时尝试选择仓库内部目标路径，确认命令明确拒绝；选择仓库外新文件名时正常继续。外部识别仍能读取通过选择器选中的仓库外图片；重启应用后旧证书路径应提示重新选择，重新选择后可继续生成预览。

## 版本结论

当前版本继续保留 `0.1.0`，尚不写入 `0.1.0-rc.1`。解除条件是以上 7 项 Windows 人工回归全部通过，并确认包含 `npm test` 的 Windows CI 全绿；之后再同步更新 `package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json` 和本 Changelog，生成内部候选安装包。公开候选还必须满足 `docs/guides/release-policy.md` 的干净 Windows 环境验收与第三方 C2PA/TrustMark 回读要求。
